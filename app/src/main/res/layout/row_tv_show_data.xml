<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="140dp"
    android:layout_height="200dp"
    android:orientation="vertical">

    <androidx.cardview.widget.CardView
        android:id="@+id/card_view"
        android:layout_gravity="center"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:foreground="?android:attr/selectableItemBackground"
        card_view:cardElevation="1dp"
        card_view:cardCornerRadius="3dp"
        android:backgroundTint="@color/template_grey_medium"
        android:layout_marginTop="@dimen/activity_vertical_margin_quarter"
        android:layout_marginBottom="@dimen/activity_vertical_margin_quarter"
        android:layout_marginEnd="@dimen/activity_vertical_margin_half"
        android:layout_marginRight="@dimen/activity_vertical_margin_half"
        android:layout_marginStart="@dimen/activity_vertical_margin_quarter"
        android:layout_marginLeft="@dimen/activity_vertical_margin_quarter">

        <ImageView
            android:id="@+id/Image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:adjustViewBounds="true"
            android:scaleType="centerCrop"
            android:layout_gravity="center"
            android:contentDescription="TV Show poster" />

    </androidx.cardview.widget.CardView>

</LinearLayout>
